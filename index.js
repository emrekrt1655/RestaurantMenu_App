/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Kahvalti from './src/Kahvalti';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Kahvalti);
